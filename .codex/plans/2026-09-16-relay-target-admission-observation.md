# Existing relay target failure classification

Owner: Laurea PR #10 for evidence model; target repository Actions administrators for execution admission; relay #3 for trusted enforcement.

## Initial subset

The first inspection covered Danse and learning-resources: six failed jobs across three runs, all with zero steps. At that intermediate stage, two of six allowlisted targets were inspected and four were uninspected. The personal Danse namesake has a different repository ID and is not substituted for the registered target.

## Retained expanded receipt

The final docs/receipts/relay-target-admission-observation-20260916.json was extended at 2026-09-16T06:11:39.701617+00:00. Its aggregate covers **six targets, seven runs and twelve jobs**, with zero targets uninspected in this sample. The initial two-target paragraph above is only a subset of that receipt, not its final denominator. All twelve jobs recorded zero executed steps; this establishes neither an executed source failure nor complete workflow/health acceptance.

On 2026-09-20 the omitted learning-resources repository identity was reconciled against the connected repository API as 1155240211. The receipt explicitly dates that identity repair separately from its original run/job observations.

Next owner diagnostic: inspect the named run/job admission details and restore an admitted execution through existing account authority. Trust acceptance still requires the registered relay profile and protected governor rail. These records are diagnostic reads, not dispatch, trusted relay execution, a seventh target, or account-mutation evidence.
